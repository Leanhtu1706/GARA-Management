@model GaraManagement.Models.Account
@{
    ViewData["Title"] = "Login";
    Layout = "";
}

<!--
Author: W3layouts
Author URL: http://w3layouts.com
-->
<!DOCTYPE html>
<html lang="zxx">

<head>
    <title>Login Gara</title>
    <!-- Meta tag Keywords -->
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta charset="UTF-8" />
    <script>
        addEventListener("load", function () {
            setTimeout(hideURLbar, 0);
        }, false);

        function hideURLbar() {
            window.scrollTo(0, 1);
        }
    </script>
    <!-- //Meta tag Keywords -->
    <!--/Style-CSS -->
    <link rel="stylesheet" href="~/css/styleLogin.css" type="text/css" media="all" />
    <script src="~/assets/js/core/jquery.min.js"></script>
    <!--//Style-CSS -->
</head>

<body>
    <!-- /login-section -->

    <section class="w3l-forms-23">
        <div class="forms23-block-hny">
            <div class="wrapper">

                <div class="d-grid forms23-grids">
                    <div class="form23">
                        <div class="main-bg">
                            <h6 class="sec-one">Login</h6>
                            <div class="speci-login first-look">
                                <img src="~/img/user.png" alt="" class="img-responsive">
                            </div>
                        </div>
                        <div class="bottom-content">
                            <form  id="formlogin" asp-action="Login" method="post">
                                <input name="username" class="input-form" placeholder="UserName"
                                       required="required" value="anhtu"/>
                                <input type="password" name="password" class="input-form"
                                       placeholder="Password" required="required"  value="123"/>

                            </form>
                            <button id="loginBtn" class="loginhny-btn btn">Login</button>
                        </div>
                    </div>
                </div>
                <div class="w3l-copy-right text-center">
                    <p>
                        © 2020 Gara Login Form. All rights reserved | Design by Tule
                    </p>
                </div>
            </div>
        </div>
    </section>
    <!-- //login-section -->
    <script>
        $('#loginBtn').click(function (e) {
            var username = $('#formlogin').find("input[name='username']").val();
            var password = $('#formlogin').find("input[name='password']").val();          
            var dataObj = {};
            dataObj.UserName = username;
            dataObj.Password = password;
            $.ajax({
                type: "POST",
                url: '@Url.Action("Login")',
                data: { account: dataObj },
                success: function (data) {
                    console.log(data);
                    if (data == "error") {
                        alert("Đăng nhập thất bại mời thử lại!");
                        //window.location.href = "Login";
                        console.log("Error");
                    }
                    else {
                        window.location.href = data.redirectToUrl;
                        console.log("Success");
                    }
                }
            });
        });
    </script>
</body>

</html>

